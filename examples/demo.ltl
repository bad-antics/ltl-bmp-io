// examples/demo.ltl — runnable demo for ltl_bmp_io

import std::io
use ltl_bmp_io as lib

fn main() {
    io::println("ltl_bmp_io demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}